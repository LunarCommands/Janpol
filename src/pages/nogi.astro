---
import Base from "@layouts/Base.astro";
import { getCollection } from "astro:content";

const furniturePosts = await getCollection("nogi");
---

<Base title="Nogi">
  <div class="container my-12 mx-auto px-4 md:px-12">
    <div class="flex flex-wrap -mx-1 lg:-mx-4">
      {
        furniturePosts.map((post) => (
          <div class="my-1 px-1 w-full md:w-1/2 lg:my-4 lg:px-4 lg:w-1/3 overflow-hidden rounded-lg shadow-lg flex flex-col justify-between">
            <a href={"/nogi/" + post.slug}>
              <img
                class="block h-full w-full object-cover object-center"
                src={post.data.images[0].src}
              />
            </a>
            <div class="p-5 flex-1">
              <a href={"/nogi/" + post.slug}>
                <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 ">
                  {post.data.title}
                </h5>
              </a>
              <p class="mb-3 font-normal text-gray-700 ">
                {post.data.description}
              </p>
            </div>
            <a
              href={"/nogi/" + post.slug}
              class="inline-flex my-2 items-center px-3 py-2 text-sm font-medium text-center text-white bg-gradient-to-r from-gray-500 via-gray-400 to-gray-300 rounded-lg hover:from-gray-400 hover:to-gray-700 focus:ring-4 focus:outline-none focus:ring-blue-300 "
            >
              Więcej
              <svg
                aria-hidden="true"
                class="w-4 h-4 ml-2 -mr-1"
                fill="currentColor"
                viewBox="0 0 20 20"
                xmlns="http://www.w3.org/2000/svg"
              >
                <path
                  fill-rule="evenodd"
                  d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z"
                  clip-rule="evenodd"
                />
              </svg>
            </a>
          </div>
        ))
      }
    </div>
  </div>
</Base>
